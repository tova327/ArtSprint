import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: { main: "#FF8C42" },
    secondary: { main: "#60A5FA" },
  },
  shape: {
    borderRadius: 10,
  },
});